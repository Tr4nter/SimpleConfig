rootProject.name = "SimpleConfig"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://maven.fabricmc.net/") // Loom plugin repository
        mavenCentral()
    }
}